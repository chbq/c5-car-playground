# Codex Task: Phase 0 Audit

Read `AGENTS.md` and `tasks/current.md` before doing anything else.

Analyze the complete C5 vendor package in `reference/c5-vendor/` and the local
Windows STM32 development environment.

Do not modify vendor files. Do not flash hardware. Do not drive motors. Do not
use C25 code.

Complete the current Phase 0 task. Run `tools/doctor.ps1`, inventory vendor
materials, produce evidence-backed hardware and pin maps, list unresolved
conflicts, and recommend the safest way to create the clean
`target/c5-firmware/` CubeMX + Keil project.

Whenever you infer something from code rather than explicit documentation,
label it as an inference. Finish with `build/task-report.md`.
